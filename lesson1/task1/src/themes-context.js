import React from 'react';

export const dark = {
  fontColor: '#fff',
  background: '#222',
};

export const light = {
  fontColor: '#000',
  background: '#eee',
};

export const ThemeContext = React.createContext(dark);
