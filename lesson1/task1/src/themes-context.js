import React from 'react';

export const dark = {
  fontColor: '#ffffff',
  background: '#222222',
};

export const light = {
  fontColor: '#000000',
  background: '#eeeeee',
};

export const ThemeContext = React.createContext(dark);
